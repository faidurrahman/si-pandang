const fetch = require('node-fetch');

async function test() {
  // Let's write a quick Google Apps Script that evaluates code and returns result... actually we can't easily.
  // But wait, getDaftarKendaraan has a similar logic:
  // let dataValues = sheetKendaraan.getRange(1, 1, lastRow, Math.max(lastColumn, 29)).getValues();
  // headers = dataValues[0];
  // for (let r = 0; r < Math.min(dataValues.length, 5); r++) {
}
